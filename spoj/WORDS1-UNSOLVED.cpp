/*
   Construct a graph from the words 

   (u,v) belongs to E if u begins with letter in which v ends or vice-versa
   Verify if the graph contains a  Euler Line. 
   	If yes the lock can be opened
	else
		No

 */

#include<iostream>

using namespace std; 

int adjacency_matrix[20][20];


int main()
{
	int t,num;
	map<string,int> wordmap;
	cin>>t;
	while(t>0)
	{
		wordmap.clear();
		cin>>num;
		

		t--;
	}

	return 0;
	
}
